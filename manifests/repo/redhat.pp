# Class: beegfs::repo::redhar

class beegfs::repo::redhat (
  $manage_repo    = true,
  $package_source = $beegfs::package_source,
  $package_ensure = $beegfs::package_ensure,
  $major_version  = $beegfs::major_version,
) {

}
